loglevel: info

http:
  port: ${port}

mongo:
  connectionstring: ${mongo_uri}
  database: ${mongo_database}
  collection: ${mongo_collection}
  username: ${mongo_username}
  password: ${mongo_password}

nats:
  url: ${nats_url}
  topic:
    emotes: ${nats_topic_emotes}

health:
  enabled: false
  port: 0

prometheus:
  enabled: false
  port: 0
